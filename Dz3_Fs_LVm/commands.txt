yes | lvremove /dev/VolGroup00/LogVol00 && lvcreate -n root -L  8G VolGroup00 && yes | mkfs.xfs /dev/VolGroup00/root && mount /dev/VolGroup00/root /mnt
xfsdump -J - /dev/vg_tmp_root/lv_tmp_root | xfsrestore -J - /mnt
for i in /proc/ /sys/ /dev/ /run/ /boot/; do mount --bind $i /mnt/$i; done
chroot /mnt/
grub2-mkconfig -o /boot/grub2/grub.cfg
cd /boot ; for i in `ls initramfs-*img`; do dracut -v $i `echo $i|sed "s/initramfs-//g; s/.img//g"` --force; done && sed -i 's/LogVol00/root/' /boot/grub2/grub.cfg
exit && shutdown -r now
yes | lvremove /dev/vg_tmp_root/lv_tmp_root && vgremove vg_tmp_root && pvremove /dev/sdc
